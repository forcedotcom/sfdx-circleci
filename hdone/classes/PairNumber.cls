public without sharing class PairNumber {
    public Integer x,y;

    /* Class used to create 2 Integers for time calculations */
    public PairNumber(Integer a, Integer b) {
        x=a;
        y=b;
    }
}